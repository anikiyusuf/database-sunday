<!DOCTYPE html>
<html lang="en">
<%-include('layouts/header.ejs') %>
<body>
     
    <div class="d-flex justify-content-end mb-4"><a class="btn btn-primary text-uppercase" href="#!">Older Posts →</a></div>
    <div class="d-flex justify-content-end mb-4"><a class="btn btn-primary text-uppercase" href="/search">Search Posts →</a></div>



    <div>
        <div>
            <div>
           
              <%if(result.length>0){%>
                <% for(let i=0; i < result.length; i++){%>
                <div class="post-preview">
                    <a href="/post/<%=result[i]._id%>">
                        <h2 class="post-title" style="font-weight:600 ;">
                                  <%=result[i].title%>
                            </h2>
                        <h5 class="post-subtitle" style="font-weight:400;">
                            <%=result[i].description%> <br>
                            <span style="font-size: 12px;color: #d63384;">Click for more...</span>
                        </h5>
                    </a>
                    <p class="post-meta">
                        Posted by
                        <a href="#!"><%=result[i].author%></a>
                        <%=result[i].createAt.toDateString()%>
                    </p>
                </div>
                <!-- Divider-->
                       <hr class="my-4" />
                <% } %>
                <% } %>
                

</body>
</html>